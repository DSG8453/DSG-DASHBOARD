{
  "buildCommand": "if [ -d frontend ]; then cd frontend; export BUILD_PATH=../build; fi && npm run build",
  "outputDirectory": "build",
  "installCommand": "if [ -d frontend ]; then cd frontend; fi && npm install --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.dsgtransport.net/api/:path*"
    }
  ]
}
